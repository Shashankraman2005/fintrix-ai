{
  "buildCommand": "cd pdr-frontend && npm install && npm run build",
  "outputDirectory": "pdr-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
